// Phase 5: AI Brief — GET endpoint placeholder
import { NextResponse } from "next/server";

export async function GET() {
  return NextResponse.json({ message: "Phase 5 — not yet implemented" }, { status: 501 });
}
